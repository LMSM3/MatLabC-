# make sure you're on main and up to date
git checkout main
git pull

# edit files: version + changelog, then commit
git add CHANGELOG.md path/to/version_file
git commit -m "Release v0.3.4"

# tag the release (annotated tag is preferred)
git tag -a v0.3.4 -m "v0.3.4"

# push commit + tag
git push
git push origin v0.3.4
