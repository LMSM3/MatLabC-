╔═══════════════════════════════════════════════════════════════════════════════╗
║                                                                               ║
║                   🎯 FINAL BUILD & SETUP - DO THIS NOW 🎯                    ║
║                                                                               ║
║                        MatLabC++ v0.3.1 - Last Mile!                         ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝


✅ STATUS: All code fixes applied, ready to build!
═══════════════════════════════════════════════════════════════════════════════

Fixed Issues:
  ✅ materials_smart.hpp - Missing #include <vector>
  ✅ package_manager.hpp - Invalid default argument syntax
  ✅ LICENSE file created
  ✅ Header files corrected

Next: Build executable and setup global integration


═══════════════════════════════════════════════════════════════════════════════
CHOOSE YOUR PATH:
═══════════════════════════════════════════════════════════════════════════════

If you have WSL (Linux on Windows):      Go to SECTION A below
If you're on Windows only:               Go to SECTION B below


═══════════════════════════════════════════════════════════════════════════════
SECTION A: WSL LINUX BUILD (Recommended for full features)
═══════════════════════════════════════════════════════════════════════════════

Step 1: Open WSL Terminal
  wsl

Step 2: Install dependencies (one time)
  sudo apt update && sudo apt install -y build-essential cmake libcairo2-dev

Step 3: Clean and build
  cd /mnt/c/Users/Liam/Desktop/MatLabC++/build
  rm -rf CMake* *.o *.so Makefile
  cd ..
  rm -rf build
  mkdir build && cd build
  cmake -DCMAKE_BUILD_TYPE=Release ..
  cmake --build . --config Release -j4

  ⏱️  This takes 5-10 minutes. Wait for: [100%] Built target matlabcpp

Step 4: Verify build
  ls -lh Release/matlabcpp.exe
  (Should show executable file)

Step 5: Setup bash integration
  cd ../v0.3.0
  bash Setup-Bash-Integration.sh
  source ~/.bashrc

Step 6: Test
  mlcpp --version
  
  ✅ Should show version info!

Step 7: Close WSL, setup Windows PATH too (optional)
  exit
  (Continue with SECTION C below)


═══════════════════════════════════════════════════════════════════════════════
SECTION B: WINDOWS ONLY BUILD
═══════════════════════════════════════════════════════════════════════════════

Requires: Visual Studio Community (with C++ compiler)
         CMake
         Admin PowerShell

Step 1: Open Admin PowerShell
  Right-click PowerShell → Run as Administrator

Step 2: Navigate to project
  cd C:\Users\Liam\Desktop\MatLabC++

Step 3: Clean and build
  rm -r build -ErrorAction SilentlyContinue
  mkdir build
  cd build
  cmake -DCMAKE_BUILD_TYPE=Release ..
  cmake --build . --config Release

  ⏱️  This takes 5-10 minutes

Step 4: Verify
  ls Release\matlabcpp.exe
  (Should show executable)

Step 5: Setup Windows PATH
  cd ..\v0.3.0
  .\Setup-Global-Integration.ps1

Step 6: CLOSE and REOPEN PowerShell
  (PATH changes require restart!)

Step 7: Test
  mlcpp --version
  
  ✅ Should work!


═══════════════════════════════════════════════════════════════════════════════
SECTION C: SETUP WINDOWS PATH (After WSL build or Windows build)
═══════════════════════════════════════════════════════════════════════════════

If you built in WSL and want Windows access too:

Step 1: Open Admin PowerShell
  Right-click PowerShell → Run as Administrator

Step 2: Go to setup script
  cd C:\Users\Liam\Desktop\MatLabC++\v0.3.0

Step 3: Run setup
  .\Setup-Global-Integration.ps1

Step 4: CLOSE PowerShell completely

Step 5: OPEN NEW PowerShell

Step 6: Test
  mlcpp --version


═══════════════════════════════════════════════════════════════════════════════
ALL-IN-ONE COMMAND (WSL - Copy & Paste Everything):
═══════════════════════════════════════════════════════════════════════════════

wsl << 'EOF'
sudo apt update && sudo apt install -y build-essential cmake libcairo2-dev
cd /mnt/c/Users/Liam/Desktop/MatLabC++
rm -rf build
mkdir build && cd build
cmake -DCMAKE_BUILD_TYPE=Release ..
cmake --build . --config Release -j4
cd ../v0.3.0
bash Setup-Bash-Integration.sh
source ~/.bashrc
mlcpp --version
EOF


═══════════════════════════════════════════════════════════════════════════════
TROUBLESHOOTING:
═══════════════════════════════════════════════════════════════════════════════

ERROR: "cmake: command not found"
  FIX: sudo apt install cmake

ERROR: "c++: command not found"
  FIX: sudo apt install build-essential

ERROR: "Build files have errors"
  FIX: rm -rf build && mkdir build && cd build
       cmake -DCMAKE_BUILD_TYPE=Release ..

ERROR: "mlcpp command not found" (after setup)
  FIX: Close and open new PowerShell/bash
       source ~/.bashrc (bash only)

ERROR: "Permission denied" (Setup-Bash-Integration.sh)
  FIX: chmod +x Setup-Bash-Integration.sh


═══════════════════════════════════════════════════════════════════════════════
EXPECTED FINAL STATE:
═══════════════════════════════════════════════════════════════════════════════

After completing above steps:

✅ mlcpp --version                    (works from any directory)
✅ mlcpp compile program.mlc          (compiles MATLAB code)
✅ mlcpp run program.m                (executes programs)
✅ Works in PowerShell, bash, VS Code (everywhere!)


═══════════════════════════════════════════════════════════════════════════════
NEXT AFTER BUILD:
═══════════════════════════════════════════════════════════════════════════════

1. Test example programs:
   mlcpp compile EXAMPLES_20260124/mlc_01_matlab_version_min.m
   mlcpp run EXAMPLES_20260124/mlc_01_matlab_version_min.m

2. Compile your own MATLAB code:
   mlcpp compile myprogram.m
   mlcpp run myprogram.m

3. Share with others:
   MatLabC++ is now installed globally on your system!


═══════════════════════════════════════════════════════════════════════════════
                    PICK YOUR PATH ABOVE AND START! 🚀
═══════════════════════════════════════════════════════════════════════════════
