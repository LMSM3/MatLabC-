🚀 WSL BUILD - COMPLETE WORKFLOW (With Fixes)
═══════════════════════════════════════════════════════════════════════════════

You're almost there! Just 5 steps:

═══════════════════════════════════════════════════════════════════════════════

STEP 1: Install Build Dependencies
─────────────────────────────────────────────────────────────────────────────

sudo apt update
sudo apt install -y build-essential cmake libcairo2-dev

(Takes 2-3 minutes, installs everything needed)

═══════════════════════════════════════════════════════════════════════════════

STEP 2: Clean Previous Build
─────────────────────────────────────────────────────────────────────────────

cd /mnt/c/Users/Liam/Desktop/MatLabC++/build
rm -rf *
cd ..

(Removes all old build files)

═══════════════════════════════════════════════════════════════════════════════

STEP 3: Configure & Build
─────────────────────────────────────────────────────────────────────────────

cd build
cmake -DCMAKE_BUILD_TYPE=Release ..
cmake --build . --config Release

(This takes 5-10 minutes. Wait for it to finish completely.)

Expected at end:
  [100%] Built target matlabcpp
  ✅ SUCCESS!

═══════════════════════════════════════════════════════════════════════════════

STEP 4: Verify Executable
─────────────────────────────────────────────────────────────────────────────

ls -lh Release/matlabcpp.exe

Should show file (not error):
  -rw-r--r-- 1 clear clear XXXXXXX Jan 24 XX:XX Release/matlabcpp.exe

═══════════════════════════════════════════════════════════════════════════════

STEP 5: Setup & Test
─────────────────────────────────────────────────────────────────────────────

cd ../v0.3.0
bash Setup-Bash-Integration.sh
source ~/.bashrc
mlcpp --version

Should show version info! ✅

═══════════════════════════════════════════════════════════════════════════════

ALL COMMANDS (Copy & Paste Together):
═══════════════════════════════════════════════════════════════════════════════

# Install dependencies
sudo apt update
sudo apt install -y build-essential cmake libcairo2-dev

# Clean old build
cd /mnt/c/Users/Liam/Desktop/MatLabC++
rm -rf build
mkdir build
cd build

# Configure
cmake -DCMAKE_BUILD_TYPE=Release ..

# Build (takes 5-10 min)
cmake --build . --config Release

# Verify
ls -lh Release/matlabcpp.exe

# Setup bash
cd ../v0.3.0
bash Setup-Bash-Integration.sh
source ~/.bashrc

# Test
mlcpp --version

═══════════════════════════════════════════════════════════════════════════════

WHAT EACH STEP DOES:

sudo apt install     → Installs cmake, build tools, cairo graphics library
rm -rf build         → Removes corrupted build files
cmake -DCMAKE...     → Configures the build system
cmake --build        → Compiles MatLabC++ (the long step)
ls Release/...       → Verifies the .exe was created
Setup-Bash...        → Makes 'mlcpp' work globally in bash
source ~/.bashrc     → Loads the new aliases
mlcpp --version      → Tests that it all works

═══════════════════════════════════════════════════════════════════════════════

TROUBLESHOOTING:

If "cmake: command not found":
  sudo apt install cmake

If "c++: command not found":
  sudo apt install build-essential

If Permission denied:
  chmod +x Setup-Bash-Integration.sh

═══════════════════════════════════════════════════════════════════════════════

YOU'RE READY! Start with Step 1 above. 🚀

═══════════════════════════════════════════════════════════════════════════════
